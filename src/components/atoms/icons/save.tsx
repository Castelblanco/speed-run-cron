import SaveIcon from '@mui/icons-material/Save';
import { SvgIconProps } from '@mui/material';

export type TIconSaveProps = SvgIconProps;
export const IconSave = (props: TIconSaveProps) => {
    return <SaveIcon {...props} />;
};
